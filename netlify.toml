[build]
  base = "frontend"
  command = "npm install && npm run build"
  # Publish directory sẽ được Netlify Next.js plugin tự động xử lý

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

